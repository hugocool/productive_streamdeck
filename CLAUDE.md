@AGENTS.md

## Supplemental: tracker skill config

Locations read by the tracker skills (wayfinder, triage, domain-modeling) when
they are used. The workflow itself is in `AGENTS.md`.

- **Issues:** GitHub on `hugocool/productive_streamdeck` via `gh`; writes need the `hugocool` token. See `docs/agents/issue-tracker.md`.
- **Triage labels:** the five defaults (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.
- **Glossary and ADRs:** single-context, `CONTEXT.md` at the root and `docs/adr/`. See `docs/agents/domain.md`.
